{
"version": "2.1",

"build": {
  "jobs": {
    "docker": [
      "image": "circleci/openjdk:11-jdk-stretch"
      ],
    "steps": [
      "checkout",
      "run: mvn package"
    ]
      }
      },
"workflows": [
"build"
]
}